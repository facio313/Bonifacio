# Install as /etc/nginx/snippets/bonifacio-sso-admin-edge-secret.conf with mode 0600.
# Replace the marker with the same random 32+ byte value stored in the admin
# container secret file. Never commit the real value.
proxy_set_header X-Portfolio-Edge-Secret "REPLACE_WITH_RANDOM_32_OR_MORE_BYTES";
